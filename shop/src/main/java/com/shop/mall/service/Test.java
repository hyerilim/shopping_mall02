package com.shop.mall.service;

public class Test {

	// 1.Dto에 params(수정데이터) 값 담기

	// 2.기존에 있던 아이디 삭제할 것인지 구분
	
	// 3.새로 저장된 이미지 파일이 있는지 확인 if(params.get("fileIdxs") != null)(새로운 이미지 추가)
	// 4.저장된 파일이 있으면 배너 정보 수정 후 수정된 배너 아이디 값을 해당하는 파일 아이디에 넣기
	// 5.새로운 파일이 없으면 값만 수정
	
}
